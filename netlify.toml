# Netlify site `masjidweb-admin-v2` is linked to this repo with base directory `admin-dashboard-v2`.
# Keep the root file minimal so UI build settings and the app-local config do not fight.

[build]
  base = "admin-dashboard-v2"

[build.environment]
  NODE_VERSION = "22"
